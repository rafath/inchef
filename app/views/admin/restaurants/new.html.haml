= simple_form_for [:admin, @restaurant] do |f|
  = f.input :name
  = f.input :domain
  = f.input :street
  = f.input :city
  = f.input :postcode
  = f.input :phone
  = f.button :submit, 'Save', class: 'btn btn-success'